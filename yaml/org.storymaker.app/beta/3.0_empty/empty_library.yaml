---
    id: empty
    title: Empty
    classPackage: scal.io.liger.model

    currentStoryPathFile: empty.json
    storyPathTemplateFiles:
        empty: empty.json
    dependencies:
        -
            dependencyId: empty
            dependencyFile: empty.json
    publishProfile:
        titlePrefix: "StoryMaker | "
        descriptionPostfix: " Created with StoryMaker 2."
        uploadSiteKeys:
            - s3
        publishSiteKeys:
            - storymaker
        tags:
            - StoryMaker
    cards:
        -
            type: LoaderHeadlessCard
            id: loader_card
            action: LOAD
            target: empty
